{
  "format_version": 1,
  "steps": [
    {
      "id": "generate",
      "source": "use mod.tool\nlet size = workflow.input.wide ? \"1536x1024\" : \"1024x1024\"\nlet response = tool.call_json(\"image.generate\", {prompt: workflow.input.prompt, size: size, quality: \"low\"})\nresponse.parse_json()"
    },
    {
      "id": "edit",
      "source": "use mod.tool\nlet handle = workflow.outputs.generate.handle\nlet vf = \"vignette\"\nif workflow.input.dark == 1 { vf = \"eq=brightness=-0.1,vignette\" }\nlet response = tool.call_json(\"media.transcode\", {src: handle, vf: vf})\nresponse.parse_json()"
    }
  ]
}
